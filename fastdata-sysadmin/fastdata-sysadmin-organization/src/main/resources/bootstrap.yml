server:
  port: ${SERVER_PORT:8010}
spring:
  application:
    name: fastdata-sysadmin-organization
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
        file-extension: yml